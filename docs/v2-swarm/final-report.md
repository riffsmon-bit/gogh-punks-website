# Gogh Art Broker V2 swarm integration report

September 13, 2026. This is a local, reviewed integration on `v2/swarm-integration-20260913`, based on production commit `eec456222bc8028df1f91b3191ce0ff8874c467e`. It is not deployed. The user's dirty working checkout and existing practice/setup services were preserved. No production NFT was burned, refund broadcast, wallet swept or broad autonomous authority activated.

1. **Agents spawned:** Thirteen distinct specialists covered the eighteen requested roles. At most three specialists ran beside the lead. Independent review found defects that the original implementation tests missed.

2. **Task assignment:** A architecture; B wallet/V1; C/H Forge and marketplace review; D real skill sourcing; E AI; F MCP; G/I discovery and links; J/K security, simulation and policy; L/O execution and persistence; M/N Control Center and Forge UI; P integration/compiler tests; Q independent security; R independent UX. [Status and ownership](status.md) records exact scopes and integration commits.

3. **Worktrees / branches:** Every specialist used an isolated `v2/swarm-*` branch and sparse checkout. The lead integrated in `/private/tmp/gogh-punk93-mint-stall`. Completed clean worktrees were removed to conserve disk; branches and commits remain. The [repository map](repository-map.md) documents existing V1/V2 code and dependencies.

4. **Completed work:** Added reviewed current-owner Agent recovery; connected Collection to recovery; hardened AI transport and completion handling; gated MCP research by equipped capabilities; corrected discovery identity/freshness and link errors; tightened safety evidence; refreshed execution authority/reserves; fixed SQL approval expiry; added a read-only archive diagnostic; improved mobile and keyboard controls; added composed tests and a compiler gate.

5. **Rejected work:** Initial recovery confirmation and sparse-array execution candidates were rejected until independently reproduced defects were fixed. Unreviewed external wallet runtimes, hash-pinned package mutation and misleading marketplace execution claims were not accepted. No specialist branch was accepted solely on its own test result. Marketplace v1 promotion remains blocked by documented price precision and order-identity/freshness limitations.

6. **Architecture decisions:** Reuse the existing schemas and server-only domain facade. Punk identity is chain/collection/tokenId; owner is a fresh observation. V3 Wallet and Agent Account custody remain distinct. Permanent progression follows the token; economic permission does not follow a sale. Skills and AI never grant wallet signing authority. Exact transaction bytes, current authority and validated simulation costs must agree. See [shared contracts](shared-contracts.md).

7. **Files changed:** Runtime changes are concentrated in `broker/src/v4`, the new Agent recovery module, bounded Netlify endpoints and the existing browser controls. Dedicated tests, review documents and safe test scripts accompany them. `package.json`/lock add pinned TypeScript 5.9.3 as a development dependency. The exact inventory is `git diff --name-only eec4562..v2/swarm-integration-20260913`.

8. **Database changes:** No migration or production database change. The generic execution store now writes the 90-second approval expiry already required by an existing migration. Discovery updates preserve canonical IDs and creation times; older observations cannot overwrite newer normalized safety state. Actual migration/SQL tests verify expiry, CAS and nonrenewing replay; disposable native PostgreSQL tests verify selected paid/burn role restrictions and journals. Deployed generic-table privileges were not audited live.

9. **Contract changes:** No Solidity, deployment manifest, ownership semantics or registered skill implementation hash changed. The existing contracts passed 284 tests plus format, high-severity lint, offline build/size and ABI checks. The burn fork fixture now deliberately establishes its paused prerequisite only on the verified disposable Anvil, because the live Forge is already enabled.

10. **AI providers:** Existing OpenAI, Anthropic, xAI, Bankr and Gemini adapters are retained. Responses are bounded by bytes and deadline, transient HTML errors can route correctly, and explicitly incomplete outputs cannot become accepted drafts. Mocked provider tests passed. Configuration/health labels are not evidence of authenticated live access to every provider. See [AI review](ai-platform-review.md).

11. **MCP status:** Authenticated selected-Punk discovery and invocation use fresh released/equipped research capabilities, runtime/package pins and ownership continuity. Missing skill reads return unavailable values. Cached acquisition/simulation data is labeled honestly. No raw key, arbitrary signer/calldata, unrestricted withdrawal or new execution tool is exposed. Required research files are included in the Netlify bundle. See [MCP review](mcp-review.md).

12. **Discovery status:** Shared ingest is retained. Canonical opportunity identity survives deduplication; stale observations cannot replace newer normalized risk evidence. Unsafe/ambiguous links and wrong-chain/testnet input fail with bounded errors. No per-Punk scanner or arbitrary website wallet-request execution was introduced. See [discovery review](discovery-review.md).

13. **Real skill sourcing status:** Eight pinned upstream repositories and sixteen surfaces were assessed for version, license, authority, security and Robinhood compatibility. Native reviewed adapters remain the integration path; external runtimes were not installed. Bankr social-posting requirements and nonstandard licenses were evaluated explicitly. See [source audit](skill-source-audit.md).

14. **Skill Forge status:** Two public RPCs verified deployed Forge code and bindings at block 62044766. Rarity Eye v1 was the sole READY registration; #93 had zero credits and learned skills, one empty slot, and #1753 remained intact. Three native research packages exist; the ten requested skills are not all accepted/live packages. Burn-to-credit passed on a disposable fork with source inventory checks explicitly mocked. That is not live inventory clearance. See [Forge/market review](forge-market-review.md).

15. **Wallet status:** Current-owner reviewed Agent ETH, EntryPoint, ERC721 and ERC1155 recovery is implemented, with exact owner destination, runtime/custody checks, simulation, immutable displayed review and recovery journals. V3 recovery is preserved. Agent arbitrary ERC20 recovery is not exposed. Receipt/event confirmation requires a separate current-custody recheck; fee-edited/replacement transactions and lost browser storage remain bounded limitations. See [wallet audit](wallet-v1-audit.md) and [recovery interface](agent-recovery-interface.md).

16. **Execution status:** Generic execution remains prepare-only. It now protects immutable envelopes, preserves unknown/sparse evidence, uses actual gas and fresh balances, refreshes authority and rejects changed bindings. The selected paid fork proved one owner confirmation, worker delivery, durable retry, lease safety, expiry and cancellation/refund fixtures. The deployed Agent account's ownership round-trip limitation remains; managed history guards are mandatory. See [execution/data review](execution-data-review.md).

17. **Control Center status:** The existing original arcade roster and all existing tabs are retained. Collection distinguishes Agent custody from V3 custody and historical acquisitions. Paid/Forge controls retain drafts and recovery hashes through errors. Fund's mobile intrinsic-width defect is fixed; roster focus/arrows and tab accessibility/visibility are implemented. The [owner test guide](../../site/broker/v2/test-guide/index.html) describes actual feature limits.

18. **Test results:** The final integrated run passed 2,153 JavaScript tests, 140 overlapping deployment checks and 284 contract tests, with zero failures or skips. Full evidence is recorded in [validation results](validation-results.json). It includes the complete serial JavaScript suite, scoped compiler gate, site build/deployment checks, contract suite, actual SQL and disposable paid/burn forks, controlled browser tests, ownership/withdrawal/AI/skills/security/idempotency regressions. The compiler gate checks selected domain consumers, not every legacy JavaScript body. See [integration validation](integration-validation.md) for precise evidence limits.

19. **Security review:** Q-01 cross-tab review substitution, Q-02 malformed terminal journals and Q-04 sparse effect evidence were independently reproduced and closed. Q-03 remains an explicit receipt-versus-custody limitation. No unresolved blocking implementation finding remains in the reviewed scope; this is not a claim of perfect safety or production acceptance. See [independent security review](security-review.md).

20. **UX review:** Actual Control Center/Forge HTML, CSS and modules were checked in isolated Chrome at 1440/768/375/320 widths with denied external requests and mocked authority boundaries. Mobile sizing, keyboard retention, selected-tab accessibility and Forge labels were corrected and retested. No generic redesign or new visual framework was introduced. See [UX review](ux-review.md).

21. **Unresolved blockers:** The archive blocker was resolved by the later minimal RPC release: Validation Cloud Free and Blockmachine are configured, the paging fix is deployed, and scheduled production checks report READY. See [RPC rollout](free-rpc-rollout.md). Floor sweeps/WETH bids are not live. Broader skill promotion, versioned marketplace precision/identity/freshness work, complete legacy asset coverage and any account epoch redesign remain separate work. See [archive evidence](archive-readiness.json).

22. **Production-canary readiness:** The integrated code is a review candidate, not a deployed release. Read-only capabilities and controlled simulations have evidence. New paid-mint funding requires the archive gate to pass. New recovery controls require deployment and an explicit small owner-wallet canary. Burn/training requires fresh named-pair inventory/obligation and transaction reviews. No broad autonomous or marketplace trading launch is approved by these results.

23. **Actions still requiring owner authorization:** Any real #1753 burn; withdrawal of #93's unused paid budget; wallet recovery/funding; a new paid-mint budget after archive repair; and any broader financial execution, skill/contract deployment or authority expansion. This swarm sent none of those production transactions. The earlier selected-owner approvals are not authority to bypass the explicit limits of this swarm task.


RPC follow-up: PR #62 published only the six-file archive hotfix and its evidence as `3b534724094aad9a775e60fe147db196272d0539`. It passed 1,901 JavaScript tests, 140 deployment checks, the extended disposable paid-mint proof and production read-only acceptance. The broader swarm integration described above remains undeployed. The next selected paid-mint test requires a new wallet budget confirmation; no production financial transaction was submitted by this rollout.
